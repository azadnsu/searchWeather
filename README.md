# Search Weather forecast

Basic tests for both e2e and API test to search weather forecast for Tallinn using Playwright Java. I have used Page Object Model(POM) in a minimal way. 

# Dependencies:
- Playwright
- TestNG

Used maven bindings. However, no reporting added. 
